{% extends 'base.html.twig' %}

{% block title %}Partner{% endblock %}

{% block body %}
    <h1>Partner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner.name }}</td>
            </tr>
            <tr>
                <th>AffiliateKey</th>
                <td>{{ partner.affiliateKey }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ partner.active ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ partner.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_index') }}">back to list</a>

    <a href="{{ path('partner_edit', {'id': partner.id}) }}">edit</a>

    {{ include('partner/_delete_form.html.twig') }}
{% endblock %}
